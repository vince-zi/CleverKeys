<?xml version="1.0" encoding="utf-8"?>
<!-- Hardware acceleration enabled for better rendering performance -->
<manifest xmlns:android="http://schemas.android.com/apk/res/android" android:hardwareAccelerated="true">
  <!-- Core keyboard permissions -->
  <uses-permission android:name="android.permission.VIBRATE"/>

  <!-- PRIVACY: No INTERNET permission - CleverKeys is fully offline, no data leaves your device -->

  <!-- Optional: Voice input support (not required for core keyboard functionality) -->
  <!-- Uncomment if you want voice typing: -->
  <!-- <uses-permission android:name="android.permission.RECORD_AUDIO"/> -->

  <!-- User dictionary access for word suggestions (system dictionary only) -->
  <uses-permission android:name="android.permission.READ_USER_DICTIONARY"/>

  <!-- For Android 11+ (API 30+), use scoped storage via MediaStore or SAF (Storage Access Framework) -->
  <!-- No explicit permission needed for app-specific directories (getExternalFilesDir()) -->

  <application android:label="@string/app_name" android:allowBackup="true" android:fullBackupContent="@xml/backup_rules" android:dataExtractionRules="@xml/data_extraction_rules" android:icon="@mipmap/ic_launcher" android:hardwareAccelerated="true">
    <!-- CleverKeys Neural Keyboard Service -->
    <!-- FIX: Removed applicationIdSuffix ".debug" from build.gradle to fix keyboard crash -->
    <service android:name="tribixbite.cleverkeys.CleverKeysService"
             android:label="CleverKeys Keyboard"
             android:permission="android.permission.BIND_INPUT_METHOD"
             android:exported="true"
             android:theme="@style/CleverKeysIMETheme"
             android:directBootAware="true">
      <intent-filter>
        <action android:name="android.view.InputMethod"/>
      </intent-filter>
      <meta-data android:name="android.view.im" android:resource="@xml/method"/>
    </service>

    <!-- Quick Settings Tile for keyboard switching (Android 7.0+) -->
    <service
        android:name="tribixbite.cleverkeys.KeyboardTileService"
        android:label="@string/app_name"
        android:icon="@mipmap/ic_launcher"
        android:permission="android.permission.BIND_QUICK_SETTINGS_TILE"
        android:exported="true">
      <intent-filter>
        <action android:name="android.service.quicksettings.action.QS_TILE"/>
      </intent-filter>
    </service>

    <activity android:name="tribixbite.cleverkeys.SettingsActivity" android:icon="@mipmap/ic_launcher" android:label="@string/app_name" android:theme="@style/settingsTheme" android:exported="true" android:directBootAware="true" android:launchMode="singleTop">
      <intent-filter>
        <action android:name="android.intent.action.MAIN"/>
      </intent-filter>
      <!-- GIF pack ZIP import via share intent -->
      <intent-filter>
        <action android:name="android.intent.action.SEND"/>
        <category android:name="android.intent.category.DEFAULT"/>
        <data android:mimeType="application/zip"/>
      </intent-filter>
      <intent-filter>
        <action android:name="android.intent.action.SEND"/>
        <category android:name="android.intent.category.DEFAULT"/>
        <data android:mimeType="application/x-zip-compressed"/>
      </intent-filter>
      <intent-filter>
        <action android:name="android.intent.action.VIEW"/>
        <category android:name="android.intent.category.DEFAULT"/>
        <data android:mimeType="application/zip"/>
        <data android:mimeType="application/x-zip-compressed"/>
      </intent-filter>
    </activity>

    <activity android:name="tribixbite.cleverkeys.LauncherActivity" android:icon="@mipmap/ic_launcher" android:theme="@style/launcherTheme" android:exported="true" android:directBootAware="true" android:windowSoftInputMode="adjustResize">
      <intent-filter>
        <action android:name="android.intent.action.MAIN"/>
        <category android:name="android.intent.category.LAUNCHER"/>
      </intent-filter>
    </activity>

    <activity android:name="tribixbite.cleverkeys.SwipeCalibrationActivity" android:label="@string/pref_swipe_calibration_title" android:theme="@style/settingsTheme" android:exported="true" android:directBootAware="true"/>

    <activity android:name="tribixbite.cleverkeys.NeuralSettingsActivity" android:label="Neural Settings" android:theme="@style/settingsTheme" android:exported="true" android:directBootAware="true">
      <intent-filter>
        <action android:name="android.intent.action.MAIN"/>
        <category android:name="android.intent.category.DEFAULT"/>
      </intent-filter>
    </activity>

    <activity android:name="tribixbite.cleverkeys.DictionaryManagerActivity" android:label="Dictionary Manager" android:theme="@style/settingsTheme" android:exported="true" android:directBootAware="true">
      <intent-filter>
        <action android:name="android.intent.action.MAIN"/>
        <category android:name="android.intent.category.DEFAULT"/>
      </intent-filter>
    </activity>

    <activity android:name="tribixbite.cleverkeys.LayoutManagerActivity" android:label="Keyboard Layouts" android:theme="@style/settingsTheme" android:exported="true" android:directBootAware="true">
      <intent-filter>
        <action android:name="android.intent.action.MAIN"/>
        <category android:name="android.intent.category.DEFAULT"/>
      </intent-filter>
    </activity>

    <activity android:name="tribixbite.cleverkeys.ThemeSettingsActivity" android:label="Keyboard Themes" android:theme="@style/settingsTheme" android:exported="true" android:directBootAware="true">
      <intent-filter>
        <action android:name="android.intent.action.MAIN"/>
        <category android:name="android.intent.category.DEFAULT"/>
      </intent-filter>
    </activity>

    <activity android:name="tribixbite.cleverkeys.ExtraKeysConfigActivity" android:label="Extra Keys Configuration" android:theme="@style/settingsTheme" android:exported="true" android:directBootAware="true">
      <intent-filter>
        <action android:name="android.intent.action.MAIN"/>
        <category android:name="android.intent.category.DEFAULT"/>
      </intent-filter>
    </activity>

    <activity android:name="tribixbite.cleverkeys.ShortSwipeCustomizationActivity" android:label="Short Swipe Customization" android:theme="@style/settingsTheme" android:exported="true" android:directBootAware="true">
      <intent-filter>
        <action android:name="android.intent.action.MAIN"/>
        <category android:name="android.intent.category.DEFAULT"/>
      </intent-filter>
    </activity>

    <activity android:name="tribixbite.cleverkeys.AutoCorrectionSettingsActivity" android:label="Auto-Correction Settings" android:theme="@style/settingsTheme" android:exported="true" android:directBootAware="true">
      <intent-filter>
        <action android:name="android.intent.action.MAIN"/>
        <category android:name="android.intent.category.DEFAULT"/>
      </intent-filter>
    </activity>

    <!-- FileProvider for sharing GIF files via content:// URIs (commitContent) -->
    <provider
        android:name="androidx.core.content.FileProvider"
        android:authorities="${applicationId}.fileprovider"
        android:exported="false"
        android:grantUriPermissions="true">
      <meta-data
          android:name="android.support.FILE_PROVIDER_PATHS"
          android:resource="@xml/gif_file_paths" />
    </provider>

    <!-- Ghost activities removed: NeuralBrowserActivity, NeuralBrowserActivityM3, TestActivity (no source files) -->

    <activity android:name="tribixbite.cleverkeys.BackupRestoreActivity" android:label="Backup &amp; Restore" android:theme="@style/settingsTheme" android:exported="true" android:directBootAware="true">
      <intent-filter>
        <action android:name="android.intent.action.MAIN"/>
        <category android:name="android.intent.category.DEFAULT"/>
      </intent-filter>
      <!-- #70: Intent filters for programmatic backup/restore automation (Termux, scripts) -->
      <intent-filter>
        <action android:name="tribixbite.cleverkeys.action.EXPORT_SETTINGS"/>
        <action android:name="tribixbite.cleverkeys.action.IMPORT_SETTINGS"/>
        <action android:name="tribixbite.cleverkeys.action.EXPORT_DICTIONARIES"/>
        <action android:name="tribixbite.cleverkeys.action.IMPORT_DICTIONARIES"/>
        <action android:name="tribixbite.cleverkeys.action.EXPORT_CLIPBOARD"/>
        <action android:name="tribixbite.cleverkeys.action.IMPORT_CLIPBOARD"/>
        <category android:name="android.intent.category.DEFAULT"/>
        <data android:scheme="file"/>
        <data android:scheme="content"/>
      </intent-filter>
    </activity>

    <activity android:name="tribixbite.cleverkeys.ShortSwipeCalibrationActivity" android:label="Short Swipe Calibration" android:theme="@style/settingsTheme" android:exported="true" android:directBootAware="true">
      <intent-filter>
        <action android:name="android.intent.action.MAIN"/>
        <category android:name="android.intent.category.DEFAULT"/>
      </intent-filter>
    </activity>

    <activity android:name="tribixbite.cleverkeys.SwipeDebugActivity" android:label="Swipe Debug Log" android:theme="@style/settingsTheme" android:exported="true" android:directBootAware="true">
      <intent-filter>
        <action android:name="android.intent.action.MAIN"/>
        <category android:name="android.intent.category.DEFAULT"/>
      </intent-filter>
    </activity>
  </application>

  <!-- To query enabled input methods for voice IME detection -->
  <queries>
    <intent>
      <action android:name="android.view.InputMethod" />
    </intent>
  </queries>
</manifest>
